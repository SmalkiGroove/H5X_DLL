---
description: CombatHero and AdvMap hero vtables — auto-synced from Hero.h
alwaysApply: true
---

# Hero Stats — CombatHero vs AdvMap

> **Auto-generated** from `src/structs/Hero.h`. Do not edit manually; run `.cursor/hooks/sync-hero-rule.py` or save `Hero.h` (Cursor hook).

In combat (e.g. `AllDamageOutputFork` in `ModDamageOutput.cpp`), unit owner `edi` exposes two hero views:

| Object | How to get | Vtable | Use |
|---|---|---|---|
| **CombatHero** | `mov edx,[edi+4]` / `mov eax,[edx+8]` / `lea ecx,[eax+edi+4]` | `0x00E8499C` | In-combat stats, skills, specs, effective spellpower |
| **AdvMap hero** | `call [edi]` (slot 0) → `eax` | `0x00E7E484` | Persistent totals (attack/def/spellpower/knowledge) |

AdvMap hero = `NWorld::CHero` subobject at `+0x1C` (RTTI `.?AVCHero@NWorld@@` @ `0x00F40324`). CombatHero reaches AdvMap hero via `mov ecx,[ecx-0x6C]` (`ICombatHero::hero()`).

## AdvMap hero vtable `0x00E7E484` — primary stat getters

All `__thiscall`, **no stack args**, result in `eax` (totals incl. artifacts/week, clamped):

| Slot | Impl | Stat |
|---|---|---|
| `0x10` | `0x00B4CC50` | **get_attack** — total, clamped >= 0 |
| `0x14` | `0x00B4CCC0` | **get_defense** — total |
| `0x18` | `0x00B4CCF0` | **get_spellpower** — total |
| `0x1C` | `0x00B4CD80` | **get_knowledge** — total, clamped >= 1 |

Other documented AdvMap slots:

- `0x08` **get_luck_raw** (`0x00B4CA10`) — unclamped, base + has_skill(SKILL_LUCK) bonus
- `0x0C` **get_morale_raw** (`0x00B4CAE0`) — unclamped, base + has_skill(SKILL_LEADERSHIP) bonus
- `0x12C` **get_mana_points** (`0x00AF8160`) — returns [this+0x140]
- `0x130` **get_max_mana** (`0x00B4C8F0`) — arg = knowledge, applies Intelligence bonus
- `0x13C` **get_luck** (`0x00B4CB40`) — get_luck_raw clamped to [-5, +5]
- `0x140` **get_morale** (`0x00B4CB60`) — get_morale_raw clamped to [-5, +5]
- `0x164` **get_spellpower_for_spell** (`0x00B4B3E0`) — arg = spellRef, returns 8 for spellpower-independent spells
- `0x174` **get_skill_mastery** (`0x00B4D150`) — also used as has_skill
- `0x190` **has_hero_spec** (`0x00B4B5A0`) — compares arg to [this+0x8C]
- `0x194` **get_hero_spec** (`0x00B4B5C0`) — returns [this+0x8C]

## CombatHero vtable `0x00E8499C` — key slots

Entries use vtordisp thunks (`sub ecx,[ecx-4]; sub ecx,NN; jmp impl`).

| Slot | Impl | Meaning |
|---|---|---|
| `0x198` | `0x00BBFE50` | **get_attack** — IHero attack + combat bonus [this+0x3C] |
| `0x19C` | `0x00BBFE60` | **get_defense** — IHero defense + combat bonus [this+0x40] |
| `0x238` | `0x00BC0700` | **get_mana** — IHero get_max_mana(knowledge) + get_mana_points() |
| `0x23C` | `0x00BBFB90` | **get_hero_level** — IHero level + combat bonus |
| `0x240` | `0x00A6A700` | **get_spell_mastery** — args = (spellRef*, flag) |
| `0x244` | `0x00A6A7E0` | **get_spellpower** — effective spellpower incl. combat buffs, args = (spellRef*, flag 0) |
| `0x290` | `0x00BBFED0` | **has_skill** — forwards to IHero get_skill_mastery (+0x174) |
| `0x294` | `0x00BBFEF0` | **has_hero_spec** — forwards to IHero has_hero_spec (+0x190) |

**No knowledge getter** on CombatHero. For knowledge in combat, use AdvMap hero `+0x1C`.

### All documented CombatHero slots

| Slot | Impl | Meaning |
|---|---|---|
| `0x188` | `0x008AEB40` | **get_atb_info** — returns [this-0x70], current ATB float at +0x1C of the result |
| `0x18C` | `0x00A6B690` | **set_atb** — value in [0..1] |
| `0x198` | `0x00BBFE50` | **get_attack** — IHero attack + combat bonus [this+0x3C] |
| `0x19C` | `0x00BBFE60` | **get_defense** — IHero defense + combat bonus [this+0x40] |
| `0x1AC` | `0x00A6BD10` | **get_luck** — get_luck_raw clamped to [-5, +5] |
| `0x1B0` | `0x00A6BD50` | **get_morale** — get_morale_raw clamped to [-5, +5] |
| `0x1B4` | `0x00BBFF00` | **get_luck_raw** — IHero raw luck + combat modifiers |
| `0x1B8` | `0x00BBFFC0` | **get_morale_raw** — IHero raw morale + combat modifiers |
| `0x1BC` | `0x00BC1EB0` | **get_initiative** — hooked by ModInitiative |
| `0x238` | `0x00BC0700` | **get_mana** — IHero get_max_mana(knowledge) + get_mana_points() |
| `0x23C` | `0x00BBFB90` | **get_hero_level** — IHero level + combat bonus |
| `0x240` | `0x00A6A700` | **get_spell_mastery** — args = (spellRef*, flag) |
| `0x244` | `0x00A6A7E0` | **get_spellpower** — effective spellpower incl. combat buffs, args = (spellRef*, flag 0) |
| `0x290` | `0x00BBFED0` | **has_skill** — forwards to IHero get_skill_mastery (+0x174) |
| `0x294` | `0x00BBFEF0` | **has_hero_spec** — forwards to IHero has_hero_spec (+0x190) |

## GetEffectiveSpellPower — CombatHero `0x244`

Impl `0x00A6A7E0`. Signature: `int __thiscall(ref, int flag)` — **two args, `ret 8`**.

`spellRef` (5 dwords on stack): `+0x00` spell obj (may NULL), `+0x04` spell id (0 = skip school bonuses).

Patch pattern (zeroed spellRef = no spell context):

```asm
xor eax, eax
push eax; push eax; push eax; push eax; push eax   ; 5-dword spellRef
; ecx = CombatHero subobject
push 0
lea eax, [esp+4]
push eax
call dword ptr [edx+0x244]   ; eax = effective spellpower
add esp, 0x14
```

## The `0x164` trap (AdvMap hero)

`0x164` (`0x00B4B3E0`) is `GetSpellPower(spellRef*)` — **`ret 4`**, one stack arg. Calling without pushing an arg desyncs the stack and may return fixed `8`. Use `+0x18` for plain total spellpower, or CombatHero `0x244` for combat-buffed value.

## `IHero` helpers (C++ patches)

- `inventory()`
- `backpack()`
- `attack()`
- `defense()`
- `spellpower()`
- `knowledge()`
- `luck()`
- `morale()`
- `mana_points()`
- `hero_class()`
- `hero_spec()`
- `has_spec()`
- `skill_mastery()`

## `ICombatHero` helpers (C++ patches)

- `hero()`
- `atb_info()`
- `atb()`
- `set_atb()`
- `attack()`
- `defense()`
- `luck()`
- `morale()`
- `initiative()`
- `mana()`
- `hero_level()`
- `spellpower()`
- `spell_mastery()`
- `has_skill()`
- `has_spec()`

## Patch cross-refs

- `ModDamageOutput.cpp` — Arcane Protection: `+0x290` + `0x244`
- `ModEffectiveSpellpower.cpp` — hooks `0x00BC00D0` skill bonus region @ `0x00BC0508`
- `ModInitiative.cpp` — hooks CombatHero `+0x1BC` (`func_GetHeroInitiative`)
